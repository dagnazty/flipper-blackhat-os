name: sd-card-image-build (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Buildroot pipeline disabled
        run: |
          echo "Buildroot workflow is disabled for this repository."
          echo "Use .github/workflows/armbian-image-build.yml for all CI builds."
